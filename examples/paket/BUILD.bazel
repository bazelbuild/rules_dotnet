load("@rules_dotnet//dotnet:defs.bzl", "fsharp_binary")

fsharp_binary(
    name = "paket2bazel.exe",
    srcs = [
        # Keep order
        "Main.fs",
    ],
    deps = [
        "@main.argu//:lib",
    ],
    target_frameworks = ["net6.0"]
)
